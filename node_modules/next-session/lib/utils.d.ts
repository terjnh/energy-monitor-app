/// <reference types="node" />
import { ServerResponse } from "http";
import { Options, SessionData } from "./types";
export declare function hash(sess: SessionData): string;
export declare function commitHeader(res: ServerResponse, name: string, { cookie, id }: SessionData, encodeFn?: Options["encode"]): void;
